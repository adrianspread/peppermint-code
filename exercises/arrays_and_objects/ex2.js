var colors = ["red", "green", "blue", "yellow", "purple", "blue"];

function reverse(arr) {
    var nevarr = arr.slice();
    return nevarr.reverse();
}

console.log(reverse(colors));
console.log(colors);
